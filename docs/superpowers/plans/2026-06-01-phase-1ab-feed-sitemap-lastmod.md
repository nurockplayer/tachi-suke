# Phase 1AB Feed Sitemap Lastmod Plan

## Plan

- [x] Add failing source/build-output tests for feed sitemap `lastmod`.
- [x] Update `sitemap.xml.ts` to compute newest public article dates globally and per locale.
- [x] Add `/feed.xml` to `sitemap.xml`.
- [x] Attach `lastmod` to global and locale feed sitemap entries.
- [x] Update docs for feed sitemap discovery.
- [x] Run verification: `pnpm install`, `pnpm test`, `pnpm check:content`, `pnpm build`, `pnpm check:links`, `pnpm check:seo`.
- [ ] Open PR, wait for checks, and merge.
