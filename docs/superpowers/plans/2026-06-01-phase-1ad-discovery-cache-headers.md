# Phase 1AD Discovery Cache Headers Plan

## Plan

- [x] Add failing build-output tests for discovery cache headers.
- [x] Update `public/_headers` for global RSS, locale RSS feeds, `llms.txt`, and search index JSON.
- [x] Document the cache behavior.
- [x] Run verification: `pnpm install`, `pnpm test`, `pnpm check:content`, `pnpm build`, `pnpm check:links`, `pnpm check:seo`.
- [ ] Open PR, wait for checks, and merge.
