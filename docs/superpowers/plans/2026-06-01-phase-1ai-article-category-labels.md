# Phase 1AI Article Category Labels Plan

## Architecture

Update `src/lib/content/article-categories.ts`, which already centralizes category title and description copy. Keep routing and content unchanged.

## Tasks

- [x] Inspect category fallback behavior.
- [x] Write failing tests for `transportation` and `procedures` category copy and built SEO output.
- [x] Add localized title/description copy.
- [x] Update docs and roadmap status.
- [ ] Run `pnpm install`, `pnpm test`, `pnpm check:content`, `pnpm build`, `pnpm check:links`, and `pnpm check:seo`.
- [ ] Open PR, wait for checks, merge when clean.
