# Phase 1AA LLM Discovery Plan

## Plan

- [x] Add failing source/build-output tests for `llms.txt`.
- [x] Implement a static Astro `/llms.txt` endpoint.
- [x] Include public locale roots, sitemap, global/locale RSS feeds, and search index URLs.
- [x] Document the discovery file in README and project docs.
- [ ] Run verification: `pnpm install`, `pnpm test`, `pnpm check:content`, `pnpm build`, `pnpm check:links`, `pnpm check:seo`.
- [ ] Open PR, wait for checks, and merge.
