# Phase 1AP Japanese/Korean Admin and Transport Articles Plan

## Architecture

Add four Markdown articles to `src/content/articles` using existing article schema, categories, and translation keys. Internal links should point only to existing generated routes in the same locale.

## Tasks

- [x] Inspect current article frontmatter, category, and content tests.
- [x] Write failing source-level and build-output SEO tests.
- [x] Add four Japanese/Korean public articles.
- [x] Update docs and roadmap/status.
- [x] Run `pnpm install`, `pnpm test`, `pnpm check:content`, `pnpm build`, `pnpm check:links`, and `pnpm check:seo`.
- [ ] Open PR, wait for checks, merge when clean.
