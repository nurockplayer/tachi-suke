# Phase 1Y Shareable Search Plan

## Plan

- [x] Add failing source/build-output tests for query-aware search.
- [x] Update `SearchPage` to render a GET search form.
- [x] Read initial `q` from `URLSearchParams`.
- [x] Sync input changes back to the URL with `history.replaceState`.
- [x] Update docs for static shareable search behavior.
- [x] Run verification: `pnpm install`, `pnpm test`, `pnpm check:content`, `pnpm build`, `pnpm check:links`, `pnpm check:seo`.
- [ ] Open PR, wait for checks, and merge.
