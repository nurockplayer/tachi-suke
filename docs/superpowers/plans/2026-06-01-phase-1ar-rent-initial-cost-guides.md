# Phase 1AR Rent Initial Cost Guides Plan

## Architecture

Use the existing Markdown article collection. Add three public article files with the same `translationKey` as the existing Traditional Chinese rental initial cost article so sitemap hreflang grouping can discover them automatically.

## Tasks

- [x] Confirm current article counts and existing zh-tw rental article.
- [x] Write failing content and sitemap expectations.
- [x] Add English, Japanese, and Korean rental initial cost articles.
- [x] Update implementation and roadmap docs.
- [x] Run `pnpm test`, `pnpm check:content`, `pnpm build`, `pnpm check:links`, and `pnpm check:seo`.
- [ ] Open PR, wait for checks, merge when clean.
