# Phase 1AG Commuter Pass Articles Plan

## Architecture

Use the existing Markdown article collection, category landing pages, RSS feed, sitemap, related-article logic, and internal-link tests. No schema changes.

## Tasks

- [x] Inspect existing article model and commuter tool route.
- [x] Write failing tests for article count, slugs, sitemap paths, category pages, and RSS inclusion.
- [x] Add zh-tw commuter pass decision article.
- [x] Add en commuter pass decision article.
- [x] Update README, docs, and roadmap status.
- [ ] Run `pnpm install`, `pnpm test`, `pnpm check:content`, `pnpm build`, `pnpm check:links`, and `pnpm check:seo`.
- [ ] Open PR, wait for checks, merge when clean.
