# Submissions Boundary

Phase 1 only ships the submit-place form UI.

Future use:

- Add place submission validation here.
- Store submissions with `pending_review` by default.
- Keep moderation separate from public pages.
- Never publish user-submitted places directly.
