# Favorites Boundary

Phase 1 only ships favorite placeholders.

Future use:

- Add favorite query and mutation helpers here.
- Use a generic `targetType` and `targetId` model.
- Require login before saving.
- Ensure users can only read and modify their own favorites.
