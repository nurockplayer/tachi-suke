# Database Boundary

Phase 1 does not connect to a database.

Future use:

- Add Supabase client creation here.
- Keep environment variable validation here.
- Keep database access functions separate from UI components.
- Enable Row Level Security before user data is shipped.
